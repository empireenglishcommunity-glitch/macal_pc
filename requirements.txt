# ═══════════════════════════════════════════════════════════════
# MACAL Agent System — Python Dependencies
# ═══════════════════════════════════════════════════════════════
# Install: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════

# ─── Core Agent Framework ─────────────────────────────────────
fastapi>=0.115.0            # Agent Daemon HTTP API
uvicorn[standard]>=0.32.0   # ASGI server for FastAPI
pydantic>=2.9.0             # Data validation & settings
pyyaml>=6.0.2               # Configuration file parsing
httpx>=0.27.0               # Async HTTP client (Ollama API calls)

# ─── File Operations & Organization ──────────────────────────
watchdog>=5.0.0             # File system monitoring (new file detection)
pymupdf>=1.24.0             # PDF text extraction (PyMuPDF/fitz)
python-docx>=1.1.0          # Word document text extraction
openpyxl>=3.1.5             # Excel file reading
Pillow>=10.4.0              # Image metadata & processing
mutagen>=1.47.0             # Audio file metadata

# ─── Windows GUI Automation ──────────────────────────────────
pywinauto>=0.6.8            # Windows GUI automation library
pyautogui>=0.9.54           # Cross-platform mouse/keyboard control

# ─── Database & Persistence ──────────────────────────────────
aiosqlite>=0.20.0           # Async SQLite (audit log, transaction journal)

# ─── Utilities ───────────────────────────────────────────────
rich>=13.9.0                # Beautiful terminal output (logs, progress)
python-dotenv>=1.0.1        # Environment variable loading (.env files)

# ─── Testing (dev only) ──────────────────────────────────────
pytest>=8.3.0               # Test framework
pytest-asyncio>=0.24.0      # Async test support
